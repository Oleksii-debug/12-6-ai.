name: NEXT100-047 Jinja Code Source Admission

on:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  qualify-jinja:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    env:
      TWELVE_SIX_EXECUTION_PROFILE: LOCAL_FREE
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 0
          persist-credentials: false

      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.11.16"

      - name: Bind exact source head
        env:
          EXPECTED_SOURCE_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
        run: test "$(git rev-parse HEAD)" = "$EXPECTED_SOURCE_SHA"

      - name: Universal exact bootstrap
        uses: ./.github/actions/execution-bootstrap
        with:
          capabilities: runtime,tests,lint
          venv: .next100-047
          command: python -m pytest
          manifest: next100-047-execution-environment.json

      - name: Static and unit gates
        run: |
          .next100-047/bin/python -m py_compile tools/validate_next100_047_jinja_code_source.py tests/test_next100_047_jinja_code_source.py
          .next100-047/bin/ruff check --select E4,E9,F tools/validate_next100_047_jinja_code_source.py tests/test_next100_047_jinja_code_source.py
          .next100-047/bin/python -m pytest -q tests/test_next100_047_jinja_code_source.py

      - name: Acquire and qualify exact upstream snapshot
        run: |
          rm -rf next100-047-evidence
          .next100-047/bin/python tools/validate_next100_047_jinja_code_source.py run \
            --repo . \
            --output-dir next100-047-evidence \
            | tee next100-047-evidence/stdout.json

      - name: Validate terminal authority
        run: |
          .next100-047/bin/python tools/validate_next100_047_jinja_code_source.py validate \
            --report next100-047-evidence/next100-047-jinja-code-admission.json
          .next100-047/bin/python - <<'PY'
          import json
          from pathlib import Path

          report = json.loads(Path("next100-047-evidence/next100-047-jinja-code-admission.json").read_text())
          assert report["verdict"] == "ADMIT"
          assert report["source_family"] == "github:pallets/jinja"
          assert report["upstream_commit"] == "5ef70112a1ff19c05324ff889dd30405b1002044"
          assert report["object_count"] == 5
          assert report["capacity_bytes"] == 238695
          assert report["independent_family_credit"] == 1
          assert report["training_purpose_decision"] == "ALLOWED"
          assert report["redistribution_decision"] == "ALLOWED_WITH_LICENSE_CONDITIONS"
          assert report["normalization_identity_preserved"] is True
          assert report["privacy_secrets_pass"] is True
          assert report["syntax_parse_compile_pass"] is True
          assert report["quality_pass"] is True
          assert report["dedup"]["exact_duplicates"] == []
          assert report["dedup"]["near_duplicate_pairs"] == []
          assert report["evaluation_reservation"]["selected_objects_are_training_only_not_evaluation_reservations"] is True
          PY

      - name: Upload immutable evidence
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: next100-047-jinja-admission-${{ github.event.pull_request.head.sha || github.sha }}
          if-no-files-found: error
          retention-days: 90
          path: |
            configs/data/next100_047_jinja_code_source_v1.json
            next100-047-execution-environment.json
            next100-047-evidence/
