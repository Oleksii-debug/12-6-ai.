# 12-6 AI — current autonomous routing

EPOCH: EPOCH-0003
STATUS: ACTIVE_AUDITED
LAST_GLOBAL_AUDIT: 2026-09-06T20:12:00Z
AUDIT_BASE_MAIN: 4d07ec1eafdb06b08ef31df97c7d091758bc35fb
CURRENT_STAGE: PRE_LEARNED_20M / RECORD_LEVEL_CORPUS_MATERIALIZATION_BLOCKED
NEXT_AUDIT_RULE: first capable worker after >=6h since LAST_GLOBAL_AUDIT, or immediately after a major-change trigger; failover audit allowed after ~8h without a valid refresh

## Authority and startup rule

Every scheduled worker, Codex Cloud run and Work run MUST read this file first, then reconstruct live GitHub before substantive work. Live exact GitHub evidence overrides this file if the project moved after this audit.

Do not continue an old task merely because a Scheduled Task prompt names it. The stable prompt defines the worker's home lane and startup algorithm; this routing epoch defines the current target.

Before mutation, before PR/update/merge action and before final verdict: perform exact + semantic ownership/collision review. Reuse existing canonical branches/PRs. The five persistent workers do not create hourly registration issues.

## Audit result — where the project actually is

The project is not waiting on model geometry. The ~20M random-init mechanics authority already exists at 20,613,440 parameters and the fail-closed launch/readiness machinery is substantially built. The learned model does not exist yet because the scientific data/exposure chain is still incomplete.

Current decisive facts:

- Source registry successor #538 @ `991a0b6e939cddeff16c075922f7c407fa1e86cb` represents 35 source objects / 15 families / 2,215,615 source-capacity bytes. This is source authority, not a training-record corpus and not tokens/loss positions.
- Global cross-source dedup incumbent #632 @ `d3333ec1b4a508df232a5aefccd6686adda745fb` has terminal scoped V7 evidence over that 35-object vector.
- DATA-526 #623 @ `559248829a2108f42b1b96ac16d01e30a2c004b3` has terminal scoped DATA-526 evidence but generic CI is red. Its retained V4 freeze is source-object/text-free authority, not a complete normalized training-record payload graph. Real record-level payload inventory + reserved-evaluation decontamination are not terminal.
- Ukrainian supply remains the limiting stratum: only ~100,856 source-capacity bytes are represented in the current 35-object authority versus the 9,000,000-byte 45% planning target. High-yield independent UA acquisition remains critical.
- D04 unique-loss/packing incumbent #498 @ `dc38ee03bc3cbdb908e418b964292aa55a85620d` has terminal scoped unique-ledger and tokenizer workflows; generic CI is red. It now has identity-safe replay/resume and next-exposure machinery, but real corpus-bound packing and positive post-pack unique causal-loss capacity remain `NOT_MATERIALIZED`.
- D05 checkpoint/resume incumbent #590 @ `294ec649e1d0d768888dfe12c00d3642d839be4b` has terminal scoped D05 S1 evidence; generic CI is red. Transactional RNG, durability primitives, production durability wiring and positive-progress resume hardening exist. It still needs current-head release-green convergence and end-to-end fresh-process equivalence bound to D04 next-exposure identity.
- D10 integration carrier #802 currently points to `70025ff4fcbc0bf85a30401ebad7b423b8d92f82` and has fresh generic CI `34057326007 = SUCCESS`. This is integration evidence only, not scientific training authority.
- D06 tokenizer-agnostic BPB metric and stricter learned-20M launch boundary have already been implemented/integrated. Do not recreate them.
- Learned-ladder verification is no longer a useful recurring worker target: current durable evidence already contains exact dedicated successful independent verification lineages for the prior ~3M/~10M claims. Re-check only if the launch authority explicitly shows those exact identities are stale or rejected.
- Training-authorized post-pack exposure remains exactly 0. No learned 20M checkpoint exists. No materially paid compute is authorized.

## Old-direction classification

### KEEP

- Research Corpus V1 convergence from exact source authority through record-level materialization, decontamination, quality/privacy, balance, cluster-safe split and deterministic builds.
- High-yield rights-clear Ukrainian acquisition and its existing Rada/ParlaMint/GRAC-family lineages; continue incumbents, do not clone them.
- D04 deterministic packing, exact loss-position accounting, replay protection, resume-safe next-exposure identity and future real-corpus binding.
- D05 checkpoint/recovery hardening, but continue the current incumbent rather than restarting the original corruption work.
- D06 contamination firewall, held-out evaluation protocol and independent scientific acceptance for the first real 20M pilot.
- D10 one-carrier integration, exact-head CI and current-main ancestry.
- Long-term Living Agent / voice / memory / self-model / world-model / continual-learning / Nika Core symbiosis as strategic vision, not as a substitute for the first learned 20M Base.

### CHANGE

- Training work: stop spending recurring cycles on another launch-gate variant. The gate exists. Worker 3 now owns trainer + checkpoint/recovery convergence + exact pilot recipe/numerics and should be ready to execute a bounded LOCAL_FREE pilot as soon as the scientific inputs become terminal.
- D04: the byte baseline and ledger framework are already established. Focus on deterministic shard/multiworker ordering, real corpus-bound packing and exact positive exposure, not another tokenizer abstraction.
- D05: the original corruption blockers are already addressed on the canonical lineage. Focus on current-head green evidence, crash/fresh-process equivalence and D04 next-exposure binding rather than rerunning the same historical audit from scratch.
- D10: keep integration productive, but add routing-audit duty only when due. Do not spend every hourly run writing coordination prose.

### STOP_STALE

Do not repeatedly rebuild/re-audit these unless live evidence materially invalidates them:

- MODEL-341 parameter-count/random-init mechanics proof;
- another learned-20M launch-gate implementation;
- tokenizer-agnostic BPB primitive;
- old five-source / ~183KB or older 31-object corpus projections;
- the historical original D05 corruption fixes as if they were still unfixed;
- unrelated open-source runtime qualification packages that are RETEST_RUNTIME_REQUIRED and do not block the 20M path;
- speculative learned 50M/100M campaign implementation before terminal measured learned-20M evidence.

### COLLISION

- Record-level corpus materialization belongs on the existing DATA-526/decontamination lineage; do not create a competing corpus-freeze implementation.
- High-yield Ukrainian source work must continue existing Rada/source owners where present; no duplicate Rada acquisition/normalization branch.
- Packing/ledger work stays on the existing D04 incumbent.
- Checkpoint/recovery stays on the existing D05 incumbent.
- Integration stays on the existing D10 carrier.
- Repository-wide CI debt is a shared D10/Codex convergence package; domain workers should fix only failures they own.

### PROMOTE

1. Exact normalized record-level payload graph bound cryptographically to the terminal source/dedup authority.
2. Reserved-evaluation decontamination against that exact record graph.
3. High-yield independent Ukrainian capacity acquisition because UA is the dominant current data bottleneck.
4. Deterministic double materialization + cluster-safe split + real D04 packing + positive exact post-pack unique-loss ledger.
5. D05 fresh-process resume equivalence bound to exact D04 next-exposure identity.
6. Independent D06 evaluation/firewall and decisive bounded-pilot acceptance protocol.
7. Shared generic-CI debt convergence so exact scientific heads can become release-green without each lane patching unrelated files.
8. Bounded LOCAL_FREE learned-20M pilot immediately when gates permit; do not insert another preparatory milestone merely to delay training.

## Current critical path

`source authority -> record-level normalized payload materialization -> evaluation reservations/decontamination -> post-composition quality/privacy + balance/family caps -> cluster-safe split -> deterministic double build -> tokenizer decision/packing -> positive exact unique causal-loss ledger -> checkpoint/recovery + exact next exposure -> independent evaluation firewall -> bounded LOCAL_FREE 20M pilot -> prove real learning/reload/resume -> full learned 20M campaign (only with required compute authorization) -> checkpoint + first-party inference/test experience -> Nika integration -> measured 50M/100M scaling`

Source bytes must never be treated as tokenizer tokens or optimized targets. Replay/padding must never manufacture capacity.

## Worker 1 — DATA

HOME: D03 / corpus / sources / provenance / dedup / decontamination.

CURRENT TARGET:

1. Continue canonical DATA-526 and existing source lineages; apply only D03-owned repairs.
2. Produce the exact dedup-surviving normalized training-record graph with stable `record_id`, source authority, family, modality, exact payload bytes/SHA-256 and canonical inventory digest.
3. Materialize it twice independently and require byte-identical inventory/output identities.
4. Hand the exact frozen pre-decontamination record identity to incumbent decontamination work and execute reserved-evaluation exact/near/fragment/code-copy exclusion before split.
5. In parallel, continue existing high-yield rights-clear UA acquisition/normalization because current UA capacity is far below target.
6. Then post-composition quality/privacy, 45/35/20 + family caps, cluster-safe split and deterministic corpus manifest.

DO NOT: create another DATA-526/decontamination/Rada implementation; count source-object metadata as training records; count source bytes as loss positions; do broad shared Ruff cleanup outside D03 ownership.

## Worker 2 — TEXT / TOKENIZER / PACKING

HOME: D04.

CURRENT TARGET:

1. Continue canonical #498; do not restart tokenizer/ledger design.
2. Terminalize only D04-owned CI failures.
3. Harden deterministic shard/multiworker ordering, document/cluster boundary preservation, truncation/padding/loss-mask semantics and resume-state serialization around the existing identity-safe next-exposure machinery.
4. Prepare an executable corpus-binding path that consumes Worker 1's immutable split/corpus identities.
5. Immediately when Worker 1 publishes terminal corpus identity: run two independent deterministic pack builds, verify exact equality, calculate real post-pack unique nonignored causal-loss positions and publish the positive ledger if the data actually supports it.
6. Fit BPE/Unigram only if the preregistered byte-baseline comparison and terminal corpus make it justified; never silently change token IDs.

DO NOT: invent capacity while D03 is nonterminal; rerun already-terminal BPB work; patch unrelated DATA/shared lint.

## Worker 3 — TRAINING + CHECKPOINT/RECOVERY

HOME: D01/D02/D05/C01 as needed for the learned-20M campaign. This worker absorbs the former standalone checkpoint-worker duties.

CURRENT TARGET:

1. Continue the canonical D05 lineage and terminalize its current-head scoped + generic evidence without restarting historical corruption work.
2. Prove uninterrupted `N` steps versus `K + checkpoint + fresh process + resume -> N` equivalence, including model/optimizer/scheduler/scaler/counters/RNG/dataloader and chronological/best/final checkpoint semantics.
3. Bind resume to Worker 2's externally authoritative exact next-exposure identity so no batch is skipped or replayed.
4. Keep AdamW as the control. Optional optimizer research such as Schedule-Free/Muon is secondary until corpus/pilot readiness; do not let it displace the critical path.
5. Maintain exact learned-20M recipe: seeds, LR/scheduler, precision, clipping, accumulation, NaN/Inf behavior, stop rules, telemetry, checkpoint cadence and LOCAL_FREE resource envelope.
6. As soon as D03/D04/D05/D06 launch inputs are terminal, execute the bounded LOCAL_FREE pilot rather than adding another gate layer. Report actual optimizer updates/loss/throughput/memory as measured evidence only.

DO NOT: self-authorize materially paid compute; repeat already-integrated launch-gate work; claim GPU evidence from CPU.

## Worker 4 — INDEPENDENT EVALUATION / SCIENTIFIC QA

HOME: D06 + independent verification surfaces. This worker must remain organizationally separate from the producer evidence it is judging.

CURRENT TARGET:

1. Own the actual contamination-safe selection-validation/final-test firewall consumed by the 20M campaign, not another generic metric primitive.
2. Bind evaluation reservations to Worker 1's exact record graph and independently verify that decontamination happened before split/packing.
3. Define/execute pilot acceptance: held-out UA/EN/code loss/BPB, random-init comparison, generation probes, memorization/exposure diagnostics, selection trajectory, reload/inference fingerprint and explicit no-final-test-access until allowed.
4. Reuse already-completed BPB and learned-ladder verification evidence; re-open those only if exact authority drift invalidates them.
5. Independently cross-check Worker 2/3 terminal artifacts needed for stage promotion, without modifying their producer implementation merely to make verification pass.
6. When the bounded pilot runs, issue a reproducible developer-validation/scientific verdict and identify exact blockers before any full learned-20M run.

DO NOT: author the producer result and then call the same work an independent audit; use final-test payload for recipe tuning; equate lower loss alone with terminality.

## Worker 5 — INTEGRATION + CI + ROUTING DUTY

HOME: D10 / COORD.

CURRENT TARGET:

1. Maintain one current-main-ancestor integration carrier; current live carrier is #802 and its present exact head has fresh generic CI success.
2. Selectively intake only collision-safe lane deltas with fresh required evidence; no wholesale divergent branch merges and no stale-green transfer.
3. Keep generic release CI separate from scoped scientific evidence.
4. Repair shared bootstrap/workflow/Ruff/policy debt centrally when it blocks the critical path; do not copy the same repair into every domain PR.
5. Control workflow fanout/backpressure and current-main ancestry.
6. Perform the global audit only when `LAST_GLOBAL_AUDIT` is >=6h old or a major-change trigger fires; after refreshing this file, immediately return to productive integration work.
7. If no valid routing refresh exists for ~8h, perform failover audit.

DO NOT: become a full-time report writer; rewrite domain logic; merge merely to show progress.

## Codex Cloud — active/next package

ACTIVE DURABLE CODEX CLAIM FOUND BY THIS AUDIT: none that supersedes the current five-worker targets.

NEXT RECOMMENDED LARGE PACKAGE:

`SHARED-CI-CONVERGENCE-V1` — converge the inherited repository-wide Ruff/bootstrap/workflow-dependency debt that makes current critical-path D03/D04/D05/D02 heads generic-red while their owned scoped tests are green. Work centrally on the canonical shared CI/control lineage (#592/#610/#652 context), preserve scientific workflow semantics, measure fanout, and do not rewrite D03/D04/D05 domain code. Checkpoint after each major repair phase. Refresh this routing file if the shared release signal materially changes.

Codex must not take Worker 1 record materialization, Worker 2 packing/ledger, Worker 3 checkpoint/pilot, or Worker 4 evaluation-firewall implementation while those are actively assigned.

## Work — active/next package

RECENT WORK-LIKE MAJOR CHANGE OBSERVED: Living Agent vision + autonomous orchestration/routing control plane were added to main. Those tasks are complete enough to stop repeating. The current routing bootstrap was the missing step and is now fulfilled by this epoch.

NEXT RECOMMENDED STRONG WORK PACKAGE:

`FIRST-CORPUS-BOUND-20M-LAUNCH-AND-PILOT-INTEGRATION` — when Workers 1–4 have terminal corpus/packing/recovery/evaluation inputs, perform a deep whole-stage verification of the exact launch packet on the single D10 carrier, resolve cross-lane incompatibilities, and execute/diagnose the bounded LOCAL_FREE 20M pilot if the executable readiness state permits. Do not create another launch-gate implementation. If invoked before those inputs are terminal, first audit live routing and take the largest disjoint cross-lane integration blocker instead of duplicating producer lanes.

A major Work result is an immediate routing-refresh trigger.

## Integration queue

1. Keep #802 current with live main and fresh generic CI.
2. Intake newer D03/D04/D05 scientific deltas only after their required exact evidence and ownership state are suitable; do not downgrade the carrier with blind red-line intake.
3. Consume Codex shared-CI convergence once terminal.
4. Compose terminal corpus + packing/exposure + recovery + eval into the existing launch authority.
5. Run bounded LOCAL_FREE pilot.
6. Only after measured terminal learned-20M: build the 50M/100M campaign from measured evidence.

## 20M / 50M / 100M state

- 20M mechanics: READY / random-init mechanics only.
- 20M learned data/exposure: BLOCKED, authorized post-pack exposure = 0.
- 20M bounded LOCAL_FREE pilot: NOT READY until terminal D03/D04/D05/D06 composition.
- 20M full learned training: NOT AUTHORIZED.
- 50M learned: PLANNING ONLY after terminal measured 20M; do not start a campaign now.
- 100M mechanics/planning may exist, but learned 100M campaign is CLOSED until terminal measured learned-20M supplies throughput, memory, loss-vs-token, validation, tokenizer, checkpoint and unique-data evidence.
- Later 1B/distributed/post-training work is future infrastructure, not the current milestone.

## Compute / owner authorization

No user action or materially paid compute is required at this audit state. Continue LOCAL_FREE engineering and bounded verification automatically.

Only ask Oleksii when a genuinely material/irreversible decision remains, especially a concrete paid-compute budget authorization after the bounded pilot and all scientific prerequisites are terminal.

## Next audit

Do not use rigid clock slots. First capable worker performs a full audit when:

- >=6h have elapsed since `2026-09-06T20:12:00Z`; or
- a major Work/Codex package completes; or
- the corpus/exposure/training gate materially changes; or
- a major blocker opens/closes; or
- collision/staleness invalidates current assignments.

If there is still no valid refresh by ~8h, first capable worker performs a failover audit.

## Owner-readable summary

Зараз 12-6 AI вже має сильну механіку моделі, контроль запуску, packing/checkpoint/evaluation infrastructure і один зелений інтеграційний кандидат. Реальна навчена 20M ще не існує. Головний шлях — перетворити дозволені джерела на точний очищений record-level корпус, отримати реальну post-pack навчальну місткість, прив'язати recovery/evaluation і одразу виконати bounded LOCAL_FREE pilot. Паралельно централізовано прибирається загальний CI-борг. Платний compute зараз не потрібен.
