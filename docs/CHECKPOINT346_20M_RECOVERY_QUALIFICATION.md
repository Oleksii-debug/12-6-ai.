# CHECKPOINT-346 — MODEL-341 bounded recovery qualification

Worker lane: `D05|CHECKPOINT-346|MODEL341-RECOVERY-QUALIFICATION|V4`

Execution profile: `LOCAL_FREE`, CPU-only, deterministic synthetic mechanics.

## Current authority

The primary MODEL-341 carrier now exists and is pinned at commit `133867d21a94637920b7a24dfc046dc09371ab5c` with exact parameter count `20,613,440` and `canonical_base=random_init`.

CHECKPOINT-346 therefore no longer fails closed for a missing primary ModelSpec. The incumbent qualification executes the real checkpoint/recovery code against that exact carrier while deliberately exposing **zero real optimized targets**.

## Observed execution before main-line convergence

Incumbent head `57572195ca0eb8713a373ab6e07a5d2917d822ec` completed dedicated GitHub Actions run `34531785636`, job `103053993265`, with verdict:

`PASS_MODEL341_RECOVERY_MECHANICS`

Observed result identity:

`62f9ff323214588845b432cb71241abbcb9ed812179a9c5a0c0eb75355a7bda5`

The run executed exactly three optimizer updates over deterministic synthetic tokens: parent step 1 before checkpoint publication, parent step 2 as the uninterrupted reference, and one step in a distinct fresh process after restoring the step-1 checkpoint. The resumed next step matched the uninterrupted reference, RNG/counters were restored, and an intentionally wrong run-manifest binding failed closed.

This V4 convergence re-roots the same incumbent onto live `main` and removes obsolete missing-ModelSpec ancestry. The dedicated workflow must pass again on the final exact PR head before integration qualification.

## Scientific credit boundary

This is recovery-mechanics evidence only:

- `real_corpus_used=false`
- `training_authorized_corpus_used=false`
- `optimized_target_exposure=0`
- `model_training_credit=false`
- `learned_weights_created=false`
- `final_test_read=false`
- `paid_compute_used=false`
- `foreign_pretrained_weights=false`

A checkpoint generated by this bounded synthetic qualification is **not** a learned model and cannot satisfy the learned-20M milestone.

## Exact carrier binding

The workflow pins and verifies:

- MODEL-341 carrier commit `133867d21a94637920b7a24dfc046dc09371ab5c`;
- `configs/candidates/model341_20m_candidate_a.json` Git blob `69e3cbd5f5c83c9d3d529a2a6376db3055979c40`;
- `src/twelve_six/model.py` Git blob `aacc0157cee08ceae5ef8a36e74bc90285c9b539`;
- `src/twelve_six/training/trainer.py` Git blob `049394ae119a5a5ac301ceded6ae6dfcae66c020`;
- `src/twelve_six/checkpoint/core.py` Git blob `8bff5f2cad79f64532f60dce3b413aa5e385d97a`;
- CPython `3.11.16` and the carrier-declared LOCAL_FREE CPU runtime lock.

The runner additionally verifies model identity, random-init identity, parameter count, checkpoint identity bindings, optimizer/trainer state, RNG restoration, and same-next-step equality.

## Integration gate

CHECKPOINT-346 is qualified for integration only when both of these are terminal on the **same final exact PR head**:

1. `CHECKPOINT-346 MODEL-341 Recovery Qualification` succeeds;
2. shared repository CI succeeds for the narrowed main-based delta.

No final-test access, long training, corpus authority, tokenizer authority, or learned-weight claim is granted by this D05 qualification.
