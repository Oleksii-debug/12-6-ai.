# Drive sync packet — 2026-09-14 — FREE-FIRST learned-20M

Purpose: one authoritative content packet for synchronizing the existing 12-6 AI Google Drive technical mirror without creating duplicate documents.

GitHub live state remains operational authority. Before editing Drive, late-bind current `main`, #548, #723, #2011 and the exact relevant PR/issue heads. Drive is a strategic/memory mirror and must never override newer GitHub SHA/CI/ownership truth.

## Existing Drive documents to update, not duplicate

Search the 12-6 AI Drive root for the existing canonical documents and patch them in place. Expected existing titles from prior project structure include:

- `CURRENT_STATE — 12-6 AI`
- `01_PROJECT_INSTRUCTIONS — 12-6 AI`
- `03_TRAINING_AND_COMPUTE_AUTHORIZATION — 12-6 AI`
- `04_MODEL_EXPORT_AND_INFERENCE_STRATEGY — 12-6 AI`
- the existing `SWARM_CONTROL` / Autopilot control document
- the current coordinator-decisions area, if present

If a title/path has changed, update the current canonical successor rather than creating a second copy.

## Patch for CURRENT_STATE — 12-6 AI

Record the following as a dated 2026-09-14 live-state section, with an explicit warning that exact GitHub live state supersedes this snapshot:

- live `main` at preparation: `0a594f91ceb61586fdf5d7062e4eae2a53ed90f7`;
- target ModelSpec remains approximately 20.613M parameters;
- no terminal learned-20M checkpoint exists;
- `AUTHORIZED_OPTIMIZED_TARGET_EXPOSURE=0`;
- `OPTIMIZER_UPDATES_EXECUTED_ON_REAL_TARGETS=0`;
- current retained corpus is not launch-authoritative because a physical Nomis1864/Verba/Sonnet-free successor is not terminal;
- the project is now in execution-first mode: mechanics/code are not counted as physical completion;
- PR #1459 remains the common scalable global-dedup throughput gate and is not execution-ready at its current head until ALL current-head HIGH findings are closed, exact-head CI is green and a new different-worker semantic audit passes;
- named high-yield zero-credit queue is preferred over new source discovery: Caselaw, PEP+LoC, Ubuntu IRC, ArXiv+LangUK, late-bound individually; Caselaw is the nearest immediately executable listed family once the common executor is terminal if other family-specific repairs remain blocked;
- PR #1256 readiness still requires same-lineage closure of tokenizer naming/identity, final PortableRunBinding/D10 consumer seam, exact-run LOCAL_FREE resource/wall-clock semantics, terminal CI and a new different-worker audit;
- unchanged PR #1336 runtime should not be repeatedly re-audited/reimplemented absent a new head or concrete new evidence;
- D05 fresh-process resume and Windows safe-stop should be closed by composing the existing lineages, not by creating framework #2;
- PR #2080 already contains launch-manifest composition mechanics but remains blocked on real upstream authorities/exposure;
- PR #2077 is the current free GitHub-hosted execution-carrier lineage but is not itself training authority and still needs current-head repair/integration closure.

## Patch for 01_PROJECT_INSTRUCTIONS — 12-6 AI

Add a binding owner priority section:

### FREE-FIRST / EXECUTION-FIRST

1. Current objective is the shortest scientifically lawful path to a genuinely learned ~20M model, not expansion of infrastructure surface area.
2. Choose work by `distance_to_first_lawful_optimizer_step`.
3. Physical terminal evidence outranks more planning once prerequisites are available.
4. Existing active Product owner wins; never duplicate an owned semantic.
5. No framework #2 for corpus, dedup, Trainer, readiness, recovery, launch manifest or evaluation.
6. No new source-family fanout while the named high-yield queue remains pending.
7. No FSDP/DeepSpeed/TorchTitan/Megatron work for the first learned-20M optimizer step.
8. No serving/post-Base/agent/alignment feature work while an executable learned-20M P0 blocker exists.
9. Reuse open-source commodity plumbing only under existing 12-6 scientific authority contracts and only when it removes a measured blocker.
10. Green CI, source bytes, mechanics tests, a PR or an audit report are not corpus/training authority by themselves.
11. Changed Product heads require fresh different-worker exact-head audit; no self-certification.
12. When positive clean optimized-target exposure and all launch gates become terminal, optimizer execution/checkpoint/resume/pilot immediately preempts optional work.

## Patch for 03_TRAINING_AND_COMPUTE_AUTHORIZATION — 12-6 AI

Add the owner compute policy dated 2026-09-14:

- Current active compute policy: **FREE ONLY / FREE-FIRST**.
- Allowed active lanes: LOCAL_FREE owner-machine compute, public-repository free hosted execution where allowed, and genuinely zero-cost/free-credit accelerators when exact science gates authorize the run.
- `PAID_COMPUTE_USED=false` remains binding.
- Do not build the current learned-20M route around paid GPU/cloud providers.
- Do not spend worker cycles selecting paid vendors, paid pricing plans or provider-specific paid infrastructure for 20M.
- After terminal learned 20M, construct the ~200M feasibility packet from measured throughput, memory, checkpoint/recovery, corpus and validation evidence. Continue free-first where credible.
- Paid compute may be evaluated technically only when measured ~200M or later ~1B evidence shows free compute is the dominant remaining constraint. Technical evaluation is not spending authorization; actual paid use still requires a separate explicit owner decision.
- 50M/100M remain optional risk-reduction probes only for a preregistered question that materially changes the ~200M decision.
- Canonical Base remains own random initialization; no foreign pretrained/instruct/aligned weights, hidden teacher logits or benchmark/final-test leakage.

## Patch for SWARM_CONTROL / Autopilot control

New workers must first read live GitHub #723, #548 and #2011. Preserve the current SWARM collision/lease/CI rules, but change candidate ranking to the following exact direct path:

```text
clean physical Nomis-free successor
-> terminal incumbent PR1459 indexed global dedup, with ALL current-head HIGH findings closed
-> already-admitted high-yield physical executions
-> reserved-eval decontamination
-> quality/privacy
-> family balance
-> cluster-safe split
-> tokenizer/packing
-> exact unique-loss ledger
-> positive optimized-target exposure
-> PR1256 readiness + D05 resume + Windows safe-stop + existing launch-manifest composition
-> independent pre-step PASS
-> one LOCAL_FREE real-target optimizer step
-> early checkpoint
-> fresh-process resume
-> bounded pilot
-> independent GO/HOLD/STOP
-> terminal learned 20M
```

Worker prohibition list:

- no duplicate #2058 narrow PR1459 repair, but do not ignore the separate #2018 current-head Python-function behavior-closure HIGH;
- no duplicate #1599 PR1256 Product repair;
- no repeat PR1336 repair/audit on an unchanged already-qualified head absent new evidence;
- no new source families while current high-yield candidates remain pending;
- no second data/dedup/trainer/readiness/recovery/evaluation/manifest architecture;
- no materially paid compute/provider work;
- no future-scale distributed-framework work for optimizer step 1;
- no serving/post-Base/agent work while direct P0 exists;
- no claim that mechanics/green CI/raw bytes equal training authority.

Required worker handoff:

```text
exact main/head SHA
ownership/lease status
CI/tests/audit state
physical evidence produced
scientific truth unchanged/changed
remaining blocker
single next trigger
```

## Patch for 04_MODEL_EXPORT_AND_INFERENCE_STRATEGY — 12-6 AI

Do not delete the strategy, but mark product-serving/export expansion as **deprioritized before terminal learned 20M**. The only inference/export work that remains on the critical path is first-party reload/fingerprint/equivalence evidence required to qualify a learned checkpoint. Serving frameworks, deployment APIs, agent features and product UX do not preempt data/exposure/training/recovery work.

## Deep-research integration guidance

The project should reuse ready FOSS selectively rather than launch migration programs.

For ~20M now:
- PyTorch-native model/trainer runtime;
- PyTorch SDPA instead of writing custom attention CUDA kernels;
- PyTorch Profiler for measured CPU/GPU/data/checkpoint bottlenecks;
- existing project checkpoint/scientific contracts, using standard PyTorch machinery underneath where parity is proven.

Use only at the measured dependency point:
- DataTrove: commodity data/dedup execution beneath existing 12-6 provenance/matcher semantics, with differential proof; not a current framework migration;
- Hugging Face Tokenizers: bounded own-corpus tokenizer A/B if the canonical decision requires it; no foreign trained tokenizer authority;
- lm-evaluation-harness: after a learned checkpoint exists, behind the project final-test/evaluation firewall;
- Accelerate/DDP/FSDP2/PyTorch Distributed Checkpoint: when measured memory/topology/portability requires them;
- TorchTitan/Megatron Core/DeepSpeed: later ~1B/multi-B measured candidates, not learned-20M blockers.

## What currently slows the project most

1. Administrative/coordination fanout and repeated ownership/audit scans.
2. Mechanics that are implemented but not physically executed on canonical data.
3. Long stale PR lineages and integration delay between repaired code and current main.
4. Applying the same heavy audit pattern to low-risk/non-authority changes as to scientific authority changes.
5. New-source/framework discovery while the existing direct-path queue is blocked on a small number of known gates.

## What accelerates the project most

1. Finish the incumbent PR1459 executor correctly once, including all current-head HIGH findings.
2. Finish the physical Nomis-free clean successor.
3. Consume existing high-yield candidates instead of discovering more.
4. Drive the survivor set through the whole downstream pipeline until authorized unique exposure becomes positive.
5. Close PR1256/D05/safe-stop/current launch composition without new architectures.
6. Execute optimizer step 1 immediately when authorized, then checkpoint -> kill/fresh resume -> bounded pilot.
7. Profile the real pilot before adding compile/checkpointing/distributed complexity.
8. Use measured terminal 20M evidence to decide direct ~200M, then later 1B.

## Coordinator decision record

Create or update the dated coordinator decision for 2026-09-14 with this headline:

`FREE-FIRST learned-20M execution directive — mechanics-to-physical-execution convergence`

Record that the owner intentionally prioritizes free compute and execution convergence now, defers materially paid compute/provider engineering, and measures success by reduction in time from mechanics-ready to physical terminal evidence and finally to the first lawful optimizer transition.
