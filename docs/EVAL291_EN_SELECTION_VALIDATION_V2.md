# EVAL-291 English Selection-Validation V2

V2 expands the immutable English selection-validation authority without reusing a training object.

- Reservation epoch: `d3f7fead8c04cafd535d1e574a7203523b54464d` at `2026-08-26T18:22:37Z`.
- Terminal selection-validation membership: inherited HTTPX timeouts, inherited Requests authentication, and Flask request-checksum documentation.
- Three independent upstream selection families are represented.
- V2 adds no final-test member. A reserved pytest candidate was rejected before seal because builder-side rights verification resolved its source payload; fail-closed treatment preserves the final-test firewall.
- Training, continued pretraining, tokenizer fit, and tokenizer adaptation are forbidden consumers.
- Flask training qualification is object-bounded to eight implementation files and explicitly excludes `docs/**`; the reserved Flask documentation blob is not a training object.
- TOK-315 currently blocks BPE fitting and reports zero selection-validation/final-test ingress.
- Exact SHA/blob identity and 5-token-shingle Jaccard (`0.85`) are the fail-closed contamination rules.
- `LOCAL_FREE` is the only execution profile used by this authority.

The terminal evidence file records the exact authority heads and live rights/source recheck used for sealing. Byte-level near-copy proof is limited to material currently available for comparison; future training or tokenizer work must re-check this reservation and fail closed on exact or near-copy overlap before any fit or training use.
