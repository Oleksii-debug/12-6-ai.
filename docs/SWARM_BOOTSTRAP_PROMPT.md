# Universal swarm bootstrap prompt

Paste the block below unchanged into each new project chat.

---

Use the connected GitHub authority for repository `Oleksii-debug/12-6-ai.`. The trailing period is part of the repository name.

Do not treat this message, old chat memory, or cached prose as current project truth. Do not ask me to assign you a task.

1. Read from LIVE `main`:
   - `docs/UNIVERSAL_SWARM_PROMPT.md`
   - `docs/SWARM_300_COORDINATION.md`
   - `configs/swarm/swarm300_protocol_v2.json` if present, or the newer canonical swarm protocol named by issue #723;
   - GitHub control issue #723.
2. Verify that issue #723 marks a swarm protocol READY for execution and that the canonical prompt and protocol agree on the active SWARM version. Prefer a newer READY version over V2 if the project has intentionally superseded V2.
3. If these authorities conflict, are unreadable, or GitHub cannot be accessed, make NO GitHub mutation and report exactly `BLOCKED_BOOTSTRAP_AUTHORITY_CONFLICT` or `BLOCKED_NO_GITHUB_AUTHORITY` as applicable.
4. Otherwise, treat the worker instructions in the live `docs/UNIVERSAL_SWARM_PROMPT.md` as your governing task. Execute them fully and autonomously NOW. Do not merely summarize, restate, critique, or propose a plan for that prompt and stop.
5. All ownership, collision, rate-limit, CI-pressure, scientific-truth, Base-lineage, compute, data, testing, late-binding, and handoff rules from the canonical live prompt are mandatory.

Begin now.
