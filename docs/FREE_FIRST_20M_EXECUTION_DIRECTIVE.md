# FREE-FIRST 20M execution directive

Status: proposed owner-requested control update, 2026-09-14.

This document narrows current project priority to the shortest scientifically lawful path from live `main` to the first learned ~20M optimizer step, early checkpoint, fresh-process resume, bounded pilot and terminal learned-20M qualification.

It does not grant corpus, tokenizer, training, optimizer-step, final-test, merge or scientific promotion authority. Exact live GitHub SHA/PR/CI/issue evidence remains authoritative over this snapshot.

## Owner policy for compute now

The active path is FREE-FIRST:

- LOCAL_FREE owner-machine CPU/GPU when available;
- public-repository GitHub-hosted free execution where allowed;
- genuinely zero-cost/free-credit accelerators only after the exact scientific launch gates authorize the run;
- no materially paid cloud/GPU spend and no provider-specific paid infrastructure on the current 20M critical path.

Paid compute is deferred from active engineering until terminal learned-20M measurements exist and the measured ~200M / ~1B feasibility route demonstrates that free compute is the dominant remaining constraint. Any future paid use still requires a separate explicit owner authorization.

## Live snapshot at creation

- repository: `Oleksii-debug/12-6-ai.`
- live `main`: `0a594f91ceb61586fdf5d7062e4eae2a53ed90f7`
- `AUTHORIZED_OPTIMIZED_TARGET_EXPOSURE=0`
- no terminal learned-20M checkpoint exists
- no real learned-target optimizer step has executed
- current retained corpus is not launch-authoritative

Late-bind every item below before action. Do not treat the hashes in this section as perpetual truth.

## Direct critical path

The only default P0 chain before optimizer step 1 is:

```text
physical Nomis1864/Verba/Sonnet-free clean successor
-> terminal performance-equivalent incumbent global dedup
-> highest-yield already-admitted physical candidate executions
-> reserved-eval decontamination
-> quality/privacy
-> family balance
-> cluster-safe split
-> deterministic tokenizer/packing decision
-> exact post-pack unique causal-loss ledger
-> positive optimized-target exposure authority
-> readiness/run binding convergence
-> D05 fresh-process resume composition
-> Windows safe-stop exact-run binding
-> one immutable launch manifest
-> independent pre-step red-team
-> one lawful LOCAL_FREE optimizer step
-> early checkpoint
-> fresh-process resume
-> bounded learned pilot
-> independent GO/HOLD/STOP
-> terminal learned ~20M
```

## Current high-value lanes

### Clean successor

Continue the existing #1951 / lawful-successor lineage. A clean successor must remove/deauthorize the prohibited Nomis1864 / Verba / Sonnet-derived source authority at the source/composition authority layer. Removing one old row, hash or alias is not sufficient. Historical contaminated V8/DATA526 contracts remain historical evidence; do not weaken them in place merely to force a clean result.

### Global dedup executor

PR #1459 is the common throughput gate. At this snapshot its current head is `fa8f9d5223fb8a4f2b56cb8f74d1940e98786dcd`, and the current repair belongs to #2058. Do not duplicate #2058 or create executor #2. Reuse the incumbent matcher semantics. Canonical physical executions wait for a repaired immutable head, terminal exact-head CI, fresh different-worker semantic audit and lawful integration/consumption.

### Capacity

Do not open new source families while the existing high-yield queue remains sufficient to justify execution first. Consume terminal eligible candidates through the incumbent matcher. Current named families include Caselaw, PEP+LoC, Ubuntu IRC and ArXiv+LangUK, but each must be late-bound for current authority/owner/blocker state. If the common executor terminalizes while other source repairs remain pending, merged Caselaw intake is the nearest immediate execution candidate at this snapshot.

### Bounded runtime

Do not create a new Trainer architecture or repeat audits of unchanged PR #1336 without concrete new evidence. Exact head `4159703623bd3aab09c4c032475633ed1d3422d5` already has existing exact-head qualification evidence and #2029 reports `NO_NEW_RUNTIME_BLOCKER`. Treat current remaining launch work as external composition/integration unless the head changes or new evidence proves otherwise.

### Readiness

Keep PR #1256 in the same lineage under #1599 / lawful successor. Do not create readiness framework #2. Current known repair themes are:

- identity-bound canonical `RETAIN_BYTE_BASELINE` tokenizer semantics;
- final revalidated `PortableRunBinding` carrying the D10 launch root directly into bounded runtime;
- truthful exact-run LOCAL_FREE resource/wall-clock semantics;
- terminal exact-head CI and fresh different-worker audit after repair.

### Recovery and safe stop

#2031 owns same-provider fresh-process resume composition with D05. #2032 owns exact current-run Windows stop-request to canonical StopLatch composition. Close those existing seams; do not create a second session/recovery system.

### Free hosted carrier

PR #2077 is the current GitHub-hosted free execution-carrier lineage. It is useful because it is manual-only, exact-SHA/manifest-bound and requires `GITHUB_HOSTED_FREE` with zero cost. It is not itself training authority and must remain fail-closed until canonical upstream launch/lease/pre-step/real-target authorities are integrated.

## Work that is currently deprioritized

While a direct-path P0 package is executable, do not start:

- new generic corpus/data frameworks;
- another dedup matcher or alternate dedup science;
- another Trainer or training framework;
- another readiness/checkpoint/evaluation system;
- broad source discovery or new source families;
- FSDP, DeepSpeed, TorchTitan or Megatron work for the first 20M step;
- multi-billion infrastructure;
- serving, post-Base, agent, alignment or product features;
- broad architecture search;
- docs-only restatements that do not remove a blocker;
- duplicate independent audits of an unchanged head already possessing the required PASS;
- framework migrations merely because an external package is popular.

## Open-source reuse policy

Prefer proven open-source commodity infrastructure under existing 12-6 authority contracts. The goal is not to maximize external dependencies; it is to avoid rewriting solved plumbing.

Current ~20M preference:

- project-native PyTorch runtime;
- PyTorch SDPA rather than a custom attention kernel;
- PyTorch Profiler for measured bottlenecks;
- existing project checkpoint/scientific contracts, with standard PyTorch mechanisms underneath when parity is proven.

Use other candidates only at the point where they remove a measured blocker:

- DataTrove: commodity data execution/dedup primitives only if they can sit below incumbent provenance/dedup semantics with differential parity; no migration project before the current executor path is terminal;
- Hugging Face Tokenizers: only for a bounded own-data tokenizer experiment if the canonical tokenizer decision requires it; never use foreign trained tokenizer state as hidden authority;
- lm-evaluation-harness: after a learned checkpoint exists, behind the project evaluation firewall;
- Accelerate/DDP/FSDP2/Distributed Checkpoint: only when measured scale/memory/topology requires them;
- TorchTitan/Megatron Core/DeepSpeed: future 1B/multi-B candidates, not current optimizer-step blockers.

## Exact autopilot worker prompt

Use this block as the routing contract for workers launched by Autopilot. It supplements, and does not replace, live #723 collision rules.

```text
12-6 AI — FREE-FIRST LEARNED-20M DIRECT-PATH WORKER

Repository: Oleksii-debug/12-6-ai.
The trailing period is part of the repository name.

Do not ask the owner to choose a task if GitHub is readable.
Do not start a new project or architecture.

FIRST, read live:
- issue #723 and current SWARM prompt/protocol;
- issue #548 and latest owner/control comments;
- issue #2011 and latest owner/control comments;
- current main SHA;
- open owners/PRs/CI for the nearest direct-path dependencies.

Then choose exactly one highest-value unowned package by DISTANCE TO FIRST LAWFUL OPTIMIZER STEP.
Existing active Product owner always wins. If occupied, do not duplicate; provide only allowed independent evidence/audit/handoff or rotate.

Priority chain:
1. clean physical Nomis-free successor;
2. terminalize/reuse incumbent PR1459 indexed global-dedup executor;
3. execute already-admitted high-yield candidates, not new source discovery;
4. finish decontam -> quality/privacy -> balance -> cluster-safe split -> tokenizer/packing -> exact unique-loss ledger;
5. obtain positive authorized optimized-target exposure;
6. converge PR1256 readiness + D05 resume + Windows safe-stop + immutable launch manifest;
7. independent pre-step PASS;
8. one LOCAL_FREE real-target optimizer step;
9. early checkpoint -> fresh-process resume -> bounded pilot -> independent GO/HOLD/STOP;
10. continue terminal learned 20M only on GO.

FREE-FIRST:
- no materially paid compute;
- no paid-provider engineering as current dependency;
- free/local/zero-cost execution only, with exact run evidence.

DO NOT:
- create framework #2 for data, dedup, Trainer, readiness, recovery or evaluation;
- open new source families while named high-yield candidates remain pending;
- duplicate #2058 PR1459 Product repair;
- duplicate #1599 PR1256 Product repair;
- reopen unchanged PR1336 audits/repair without new concrete evidence;
- build FSDP/DeepSpeed/TorchTitan/Megatron for the first 20M step;
- work on serving/post-Base/agent features while an executable P0 blocker exists;
- count green CI, raw source bytes or mechanics as training authority;
- self-certify a changed Product head.

Prefer physical execution and exact evidence over more planning once prerequisites are terminal.
When positive exposure + terminal launch authority exist, optimizer execution preempts optional work immediately.

Your final handoff must state exact head/SHA, ownership, tests/CI, physical evidence, remaining blocker and the single next trigger. More PRs are not success; shortening the executable chain is success.
```

## Scaling after terminal 20M

After terminal learned ~20M, build the ~200M feasibility packet from measured evidence. Do not automatically train 50M or 100M. Use an intermediate scale only for a preregistered question that materially changes the ~200M decision.

Continue free-first at ~200M where technically credible. Evaluate paid compute only later if measured evidence shows free resources, rather than data/runtime correctness, are the dominant constraint. ~1B and multi-B distributed stacks are future measured gates, not reasons to delay the first learned model.

## Success metric

The project is not accelerated by maximizing issues, PRs, frameworks or audit prose. It is accelerated when the time between `mechanics ready` and `physical terminal evidence integrated` shrinks, culminating in:

```text
positive authorized exposure
-> optimizer step 1
-> checkpoint
-> fresh resume
-> bounded pilot
-> terminal learned 20M
```
