# NEXT100-063 source registry V6 — terminal code intake handoff

This current-main successor preserves the incumbent NEXT100-063 source-registry authority
without importing the obsolete 2026-08-26 branch ancestry.

V5 remains immutable by exact Git blob
`2dcc57cfba8ab6d600bc431a8713f7b8e305dcbf`. Its pre-successor-global-dedup
credited planning vector remains 2,215,615 bytes across 15 independent families,
including 276,466 code bytes across six code families.

PR #818 adds terminal LOCAL_FREE intake evidence for six different permissive Python
families: Flask, Click, Jinja, Werkzeug, AnyIO and pytest. Two fresh materializations
were byte-identical and measured 229 eligible implementation files / 3,880,009 UTF-8
bytes. The retained evidence is bound by exact workflow, artifact, digest and evidence
blob identity.

## Critical truth boundary

The #818 source contract explicitly forbids automatic canonical capacity credit.
Therefore V6 **does not** add 3,880,009 bytes to the credited corpus figure.

Current V6 state is:

- credited pre-global-dedup planning capacity: 2,215,615 bytes;
- terminal new intake pending global dedup: 3,880,009 bytes;
- potential pre-dedup envelope if every pending byte survives: 6,095,624 bytes;
- credited code capacity: 276,466 bytes;
- potential code envelope if all pending bytes survive: 4,156,475 bytes;
- authorized unique optimized-target exposure: 0.

The historical 4,000,000-byte code acquisition-planning target is therefore potentially
met before global dedup, but this is not a canonical post-dedup capacity claim.

## Exact next action

Incumbent global-dedup PR #632 must consume both the exact V5 registry blob and the exact
#818 terminal-intake evidence, reconstruct or fetch the 229-file payload graph, and run
global exact/near/fragment/lineage dedup. Only its terminal post-dedup record inventory
may promote surviving bytes to the next corpus-capacity authority.

After that, reserved-evaluation decontamination, post-composition quality/privacy,
balance/family caps, cluster-safe split, deterministic sharding/packing, two clean corpus
builds and the post-pack unique causal-loss ledger remain mandatory.

No tokenizer fit, optimizer update, model training, final-test access, learned-20M
promotion or paid compute is authorized by V6.
