# NEXT100-063 Research Corpus V1 source registry V6

V6 is the clean selective successor to historical PR #538. It does not revive that PR's
old dedicated-workflow fanout. Instead it binds V5 by exact PR head and immutable Git
blob, then consumes only the new terminal source-intake evidence merged by PR #818.

## What changes

Historical V5 planning authority remains 2,215,615 bytes across 15 independent families.
PR #818 adds 3,880,009 strict-UTF-8 Python implementation bytes across six new,
non-overlapping permissive-license families, with two byte-identical fresh
materializations and a terminal credential scan.

The resulting **pre-global-dedup planning vector** is:

- Ukrainian: 100,856 bytes / 4 families;
- English: 1,838,293 bytes / 5 families;
- code: 4,156,475 bytes / 12 families;
- total: 6,095,624 bytes / 21 families;
- historical 20,000,000-byte acquisition-planning gap: 13,904,376 bytes.

These are source-intake planning bytes only. They are not tokenizer tokens, packed
causal-loss positions, final corpus capacity, epochs, FLOPs, or learned-model evidence.

## Why V6 is safe

The validator binds the historical V5 PR/head/blob, the exact merged #818 contract and
terminal evidence Git blobs, execution SHA, workflow run, retained artifact digest,
family list, file/byte arithmetic, two-build evidence, license/source checks, and the
zero-training claim boundary. It also rejects any overlap with the six code families
already credited by V5.

## Required next gate

The next highest-value D03 step is one composed global exact/near/lineage dedup over the
V6 candidate graph. Only its terminal post-dedup record inventory may advance to
reserved-evaluation decontamination, post-composition quality/privacy, balance/family
caps, cluster-safe split, deterministic packing, two clean corpus builds, and the exact
post-pack unique causal-loss ledger.

Until that chain is terminal:

- authorized balanced no-replay loss positions = 0;
- tokenizer fitting is blocked;
- learned-20M optimizer updates are blocked;
- paid compute is not authorized.

LOCAL_FREE only. No final-test payload is used by this package.
